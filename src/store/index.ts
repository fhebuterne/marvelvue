import {createStore} from 'vuex'
import CounterModule from "@/store/CounterModule";

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    CounterModule
  }
})
